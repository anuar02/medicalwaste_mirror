stages:
  - build
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: ""

# Build frontend Docker image
build-frontend:
  stage: build
  image: docker:20.10.16
  services:
    - docker:20.10.16-dind
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - cd frontend
    - docker build -t $CI_REGISTRY_IMAGE/frontend:$CI_COMMIT_SHA .
    - docker push $CI_REGISTRY_IMAGE/frontend:$CI_COMMIT_SHA
  only:
    - main

# Build backend Docker image
build-backend:
  stage: build
  image: docker:20.10.16
  services:
    - docker:20.10.16-dind
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - cd backend
    - docker build -t $CI_REGISTRY_IMAGE/backend:$CI_COMMIT_SHA .
    - docker push $CI_REGISTRY_IMAGE/backend:$CI_COMMIT_SHA
  only:
    - main

# Build nginx Docker image
build-nginx:
  stage: build
  image: docker:20.10.16
  services:
    - docker:20.10.16-dind
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - cd nginx
    - docker build -t $CI_REGISTRY_IMAGE/nginx:$CI_COMMIT_SHA .
    - docker push $CI_REGISTRY_IMAGE/nginx:$CI_COMMIT_SHA
  only:
    - main

# Deploy to Docker Swarm
deploy-swarm:
  stage: deploy
  tags:
    - docker
    - swarm
  script:
    - echo "Preparing for deployment..."
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
    - envsubst < docker-compose.yml > docker-compose.prod.yml
    - docker stack deploy -c docker-compose.prod.yml --with-registry-auth smart-bin
    - sleep 20
    - echo "Checking service status:"
    - docker stack services smart-bin
    - echo "Checking service logs:"
    - for service in $(docker stack services smart-bin --format "{{.Name}}"); do
    -   echo "=== Logs for $service ==="
    -   docker service logs --tail 20 $service || echo "Could not get logs"
    - done
  environment:
    name: production
  only:
    - main